# 斐波那契数列实现

一个教学级的 Python 斐波那契数列实现，强调代码可读性、输入验证和测试覆盖。

## 项目简介

本项目实现了一个高效的斐波那契数列函数，使用**迭代法**实现，具有以下特点：

- ✅ **类型注解**: 完整的类型提示（Type Hints）
- ✅ **输入验证**: 严格的类型和范围检查
- ✅ **错误处理**: 清晰的异常消息
- ✅ **中文文档**: 详细的注释和文档字符串
- ✅ **全面测试**: 100% 测试覆盖率（unittest）
- ✅ **PEP 8 规范**: 符合 Python 代码风格指南

## 算法说明

### 斐波那契数列定义

```
F(0) = 0
F(1) = 1
F(n) = F(n-1) + F(n-2)  (n >= 2)
```

### 实现方式

本项目使用**迭代法**实现，具有以下优势：

- **时间复杂度**: O(n)
- **空间复杂度**: O(1)
- **无栈溢出风险**: 相比递归法更安全
- **适合教学**: 代码逻辑清晰易懂

## 安装说明

本项目仅使用 Python 标准库，无需安装第三方依赖。

**要求**: Python 3.6+

## 使用方法

### 基本用法

```python
from fibonacci import fibonacci

# 计算斐波那契数
print(fibonacci(0))   # 输出: 0
print(fibonacci(1))   # 输出: 1
print(fibonacci(10))  # 输出: 55
print(fibonacci(50))  # 输出: 12586269025
```

### 错误处理

```python
from fibonacci import fibonacci

# 负数输入
try:
    fibonacci(-1)
except ValueError as e:
    print(e)  # 输出: n must be a non-negative integer

# 浮点数输入
try:
    fibonacci(3.14)
except TypeError as e:
    print(e)  # 输出: n must be an integer

# 字符串输入
try:
    fibonacci("hello")
except TypeError as e:
    print(e)  # 输出: n must be an integer
```

## 运行测试

### 运行所有测试

```bash
# 方法 1: 使用 unittest 模块
python -m unittest test_fibonacci.py -v

# 方法 2: 直接运行测试文件
python test_fibonacci.py
```

### 预期输出

```
test_base_cases ... ok
test_boolean_input ... ok
test_float_input ... ok
test_large_number ... ok
test_medium_numbers ... ok
test_negative_input ... ok
test_sequence_consistency ... ok
test_small_numbers ... ok
test_string_input ... ok
test_zero ... ok

----------------------------------------------------------------------
Ran 10 tests in 0.000s

OK
```

## 测试覆盖

本项目包含 10 个测试用例，覆盖以下场景：

| 测试类别 | 测试用例 | 描述 |
|---------|---------|------|
| 基础功能 | `test_base_cases` | F(0) 和 F(1) |
| | `test_small_numbers` | F(5) 和 F(10) |
| | `test_medium_numbers` | F(15), F(20), F(25) |
| 边界情况 | `test_zero` | F(0) 最小有效值 |
| | `test_large_number` | F(50) 性能验证 |
| 错误处理 | `test_negative_input` | 负数 → ValueError |
| | `test_float_input` | 浮点数 → TypeError |
| | `test_string_input` | 字符串 → TypeError |
| | `test_boolean_input` | 布尔值 → TypeError |
| 递推验证 | `test_sequence_consistency` | F(n) = F(n-1) + F(n-2) |

## 文件结构

```
fib/
├── fibonacci.py          # 主实现文件（核心函数）
├── test_fibonacci.py     # 单元测试文件
├── README.md             # 项目说明文档
├── prompt.md             # 优化后的需求文档
├── task_plan.md          # 任务执行计划
├── findings.md           # 技术调研报告
├── progress.md           # 进度追踪记录
└── test_results.log      # 测试运行日志
```

## 代码风格

本项目严格遵循 PEP 8 规范：

- ✅ 使用类型注解（Type Hints）
- ✅ 完整的 docstring（Google 风格）
- ✅ 清晰的变量命名
- ✅ 详细的中文注释
- ✅ 适当的异常处理

## 性能说明

| n 值 | F(n) | 计算时间 |
|-----|-----|---------|
| 10 | 55 | < 0.001s |
| 50 | 12,586,269,025 | < 0.001s |
| 100 | 354,224,848,179,261,915,075 | < 0.001s |

*注：Python 自动处理大整数，无需特殊处理。*

## 最佳实践演示

本项目展示了以下 Python 最佳实践：

1. **类型注解**: 使用 `def fibonacci(n: int) -> int:` 提高代码可读性
2. **防御性编程**: 先验证输入，再执行逻辑
3. **异常处理**: 使用明确的异常类型和错误消息
4. **文档字符串**: 包含参数、返回值、异常和示例
5. **单元测试**: 100% 覆盖所有代码路径

## 常见问题

### Q: 为什么不使用递归实现？

A: 递归实现的时间复杂度是 O(2^n)，且存在栈溢出风险。迭代实现更高效、更安全，且代码同样清晰。

### Q: 为什么拒绝布尔值输入？

A: 虽然 `bool` 是 `int` 的子类，但 `fibonacci(True)` 在语义上是不合理的。这是防御性编程的最佳实践。

### Q: 如何支持更大的 n 值？

A: 本实现已可支持任意大的 n 值（受限于计算时间），Python 自动处理大整数。如需更高性能，可使用矩阵快速幂或公式法。

## 作者

本项目由 Claude Code 助手生成，作为 Python 教学示例。

## 许可证

本项目仅供学习和教学使用。

---

**Happy Coding! 🚀**
